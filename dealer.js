class Dealer {
  constructor() {
    this.hand = []
  }

  hit() {
    //runs dealACard() from Blackjack class
    //if countScore() is below or equal 16, run hit again
    //if countScore() is above 16, return score
  }
}

module.exports = Dealer
